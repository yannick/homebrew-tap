# homebrew-tap

Personal Homebrew tap.

```sh
brew tap yannick/tap
brew install mlx-serve
```

Formulas are bumped automatically by each project's release pipeline;
binary assets live on GitHub releases in this repo.
